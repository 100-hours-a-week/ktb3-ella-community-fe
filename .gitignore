# Dependencies (의존성 모듈 - 용량이 크고 재설치 가능하므로 제외)
node_modules/
.pnp
.pnp.js

# Production (빌드 결과물 - Vite는 dist, CRA는 build 사용)
/dist
/build

# Local Env Files (환경변수 - API 키 등 보안 정보가 포함될 수 있어 제외 필수)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs (에러 로그)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing (테스트 커버리지 리포트)
/coverage

# Editor & OS (에디터 설정 및 운영체제 임시 파일)
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?
